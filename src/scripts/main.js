'use strict';
// const langSwitcher = document.querySelector('.langSwitcher');
// const trigger = langSwitcher.querySelector('.langSwitcher__trigger');

// trigger.addEventListener('contextmenu', (event) => {
//   event.preventDefault();

//   // langSwitcher.classList.toggle('langSwitcher--active');

//   if (langSwitcher.classList.contains('langSwitcher--active')) {
//     langSwitcher.classList.remove('langSwitcher--active');
//   } else {
//     langSwitcher.classList.add('langSwitcher--active');
//   }
// });
